name: code coverage
on:
  push:
    branches:
      - main
jobs:
  coverage-function-hcl:
    name: function-hcl coverage
    if:  github.repository == 'crossplane-contrib/function-hcl'
    runs-on: ubuntu-latest
    steps:
      - id: checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: "0"
      - id: go
        uses: actions/setup-go@v5
        with:
          go-version: 1.25
      - name: run tests
        working-directory: function-hcl
        run: |
          go test ./internal/... -coverprofile=cover.out
      - name: update coverage report
        uses: ncruces/go-coverage-report@v0
        with:
          coverage-file: function-hcl/cover.out
          amend: false
          reuse-go: true
        continue-on-error: true

  coverage-function-hcl-ls:
    name: function-hcl-ls coverage
    if:  github.repository == 'crossplane-contrib/function-hcl'
    runs-on: ubuntu-latest
    steps:
      - id: checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: "0"
      - id: go
        uses: actions/setup-go@v5
        with:
          go-version: 1.25
      - name: run tests
        working-directory: function-hcl-ls
        run: |
          go test ./... -coverprofile=cover.out
      - name: update coverage report
        uses: ncruces/go-coverage-report@v0
        with:
          coverage-file: function-hcl-ls/cover.out
          output-dir: function-hcl-ls
          amend: false
          reuse-go: true
        continue-on-error: true
